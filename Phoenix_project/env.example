这是您的 .env 文件。请填入您的实际密钥。

这个文件不应该被提交到版本控制中。

--- AI / LLM 密钥 ---

(来自 system.yaml -> ai.ai_providers)

GEMINI_PRO_KEY="<填入您的 Gemini Pro API 密钥>"

(来自 env.example - 用于成本分散)

GEMINI_EMBEDDING_KEY="<填入您的 Gemini Embedding API 密钥>"
GEMINI_FLASH_KEY="<填入您的 Gemini Flash API 密钥>"

(来自 RAG_ARCHITECTURE.md 和 memory/vector_store.py)

PINECONE_API_KEY="<填入您的 Pinecone API 密钥>"

(env.example 中的备用项)

OPENAI_API_KEY="<填入您的 OpenAI API 密钥 (如果使用)>"

--- 数据库 URI (用于 Docker Compose) ---

这些 URI 应该使用 docker-compose.yml 中定义的服务名称作为主机名

(来自 system.yaml -> tabular_db.uri_env)

示例: "postgresql://phoenix_user:YOUR_NEO4J_PASSWORD@phoenix_postgres:5432/phoenix_tabular"

TABULAR_DB_URI="<填入您的 PostgreSQL 连接 URI (使用 phoenix_postgres 主机名)>"

(来自 system.yaml -> temporal_db.uri_env)

示例: "http://phoenix_elastic:9200" (如果使用 ES) 或 "http://phoenix_influxdb:8086" (如果使用 Influx)

TEMPORAL_DB_URI="<填入您的时序数据库 URI (例如 http://phoenix_elastic:9200)>"
TEMPORAL_DB_TOKEN="<填入您的 InfluxDB 令牌 (如果使用 InfluxDB)>"

--- 数据库密码 (用于 Docker Compose) ---

(来自 system.yaml -> neo4j_db.password_env)

这必须与 docker-compose.yml 中 NEO4J_AUTH 的密码部分匹配 (例如 "password")

NEO4J_PASSWORD="password"

(来自 docker-compose.yml - postgres_db 服务)

这必须与 docker-compose.yml 中 POSTGRES_PASSWORD 匹配 (例如 "changeme_in_prod")

并且应该包含在上面的 TABULAR_DB_URI 中

POSTGRES_PASSWORD="changeme_in_prod"

--- 券商 API 密钥 (Broker API Keys) ---

(来自 system.yaml -> broker)

ALPACA_API_KEY="<填入您的 Alpaca API Key>"
ALPACA_API_SECRET="<填入您的 Alpaca API Secret>"

--- 传统数据源 API 密钥 (来自 env.example) ---

ALPHA_VANTAGE_API_KEY="<填入您的 Alpha Vantage 密钥>"
TWELVEDATA_API_KEY="<填入您的 Twelve Data 密钥>"
TIINGO_API_KEY="<填入您的 Tiingo 密钥>"

--- 遗留的 URI (来自 env.example, 可能已被上面的 URI 替代) ---

POSTGRES_DB_URL="postgresql://user:password@host:port/database"
ELASTICSEARCH_URL="http://localhost:9200"
